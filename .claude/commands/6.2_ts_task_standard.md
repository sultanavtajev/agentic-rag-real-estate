## Oppgave
$ARGUMENTS

## Arbeidsmetodikk
- Intervju meg ved å bruke AskUserQuestion for å bygge spec
- La oss gå gjennom funksjonaliteten fra start til slutt som en bruker. Steg en: hva ser vi og hva bør vi se. Steg to: hva ser vi og hva bør vi se. Og slik videre med så mange steg som behøves.
- La oss dele denne oppgaven i så små deler som mulig.
- Vær grundig.
- Atomære enheter: Én komponent/funksjon = én oppgave

## Sub-agents
- Bruk sub-agents for parallelle eller uavhengige oppgaver som kan utføres samtidig
- Deleger til sub-agents når:
  - Oppgaver kan kjøres uavhengig av hverandre
  - Det er research/analyse som ikke blokkerer hovedflyten
  - Testing eller validering av isolerte komponenter
  - Generering av dokumentasjon eller typer
- Hver sub-agent får ett klart, avgrenset ansvar
- Koordiner resultater fra sub-agents før neste hovedsteg

## Kodestruktur
- Les `docs/GUIDELINES.md` for alle retningslinjer (filstørrelser, navngivning, typesikkerhet, feilhåndtering, kodestil)
- Frontend: `frontend/` med Next.js (App Router)
- Komponenter: `frontend/components/`
- Sidekomponenter: `frontend/app/{side}/page.tsx`
- Typer: `frontend/lib/types.ts`
- API-klient: `frontend/lib/api-client.ts`
- UI-primitiver: `frontend/components/ui/`
- Maks 200 linjer per fil, 150 for feature-komponenter — del opp ved behov
- Ingen `any` — bruk typed interfaces for props og API-responser
- Alle API-typer i `frontend/lib/types.ts`
- Sjekk at alle deler som blir påvirket av endringen blir tilpasset, både backend og frontend

## UI/UX
- Alt UI/UX skal være responsivt og fungere på alle skjermstørrelser (mobile-first)
- Alt skal være strukturert, oversiktlig og intuitivt
- Bruk Shadcn-komponenter hvis det finnes ferdige. Lag nye komponenter i Shadcn-stil ved behov.
- Tilgjengelige komponenter: `Card`, `Table`, `Badge`, `Button`, `Input`, `Tabs`, `Switch`

## Kvalitet
- TypeScript: `cd frontend && npm run lint`
- Python: `ruff check src/ api/ tests/ --fix` + `ruff format src/ api/ tests/` (kun ved backend-endringer)
- Kjør `pytest` og verifiser at alle tester passerer (kun ved backend-endringer)

## Sjekkliste
- Opprett en egen mappe for denne oppgaven under `docs/teknisk/sjekkliste/{oppgave-navn-i-kebab-case}/`
- Lag `hovedsjekkliste.md` i mappen med alle steg
- Marker oppgaver som egner seg for sub-agents med [SUB-AGENT] i sjekklisten
- Oppdater sjekklisten etterhvert som oppgaver fullføres

## Når alt i sjekklisten er ferdig implementert:
- Kjør `cd frontend && npm run lint` og fiks eventuelle feil
- Kjør `ruff check src/ api/ tests/ --fix && ruff format src/ api/ tests/` (hvis backend ble endret)
- Kjør `pytest` og verifiser at alle tester passerer (hvis backend ble endret)
- Commit alle endringer via `/3.0_commit_all`
- Ikke merge til andre brancher og ikke push
- Legg til "OK - " først i mappenavnet til oppgavens sjekkliste-mappe
